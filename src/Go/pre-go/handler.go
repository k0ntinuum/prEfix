package main
import (
	//"math/rand"
	//"strings"
	//"fmt"
)

// func random_mode(handlers, max int) handler {
// 	inputs  :=
// 	outputs := pre(handlers,max)
// 	return h

// }
// func random_inputs(extra, length int) (inputs []string) {
// 	for i := 0; i< extra; i++ { inputs = append(inputs, word(rand.Intn(length) + 1))}
// 	inputs = append(inputs, "O","|");return
// }

// func random_filter(modes int) filter {

// }

// func Perm(n int) []int